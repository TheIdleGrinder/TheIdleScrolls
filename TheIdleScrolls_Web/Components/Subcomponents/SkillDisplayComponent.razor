@using TheIdleScrolls_Core.Skills


<div class="skill-progress-bar-container" style="margin-top: 0.5rem;">
    @Skill.Name
    <div id="skill-bar-frame" class="progressFrame withTooltip" style="justify-content: start">
        <div id="skill-bar-fill" class="progressFill" style="width: @($"{CalculateProgress():0%}");"></div>
        <span class="barTooltip">
            @foreach (var effect in Skill.Effects)
            {
                @foreach (var line in effect.Description.Split('\n'))
                {
                    @line<br />
                }
            }
        </span>
    </div>
</div>

<style>
    .skill-progress-bar-container {
        display: inline-flex;
        gap: 0.5rem;
        font-size: 0.75rem;
        width: 90%;
        flex-direction: column;
        align-self: center;
    }

    #skill-bar-frame {
        width: 100%;
        border: 1px solid var(--frame-color);
        background: inherit;
        height: fit-content;
        align-self: center;
    }

    #skill-bar-fill {
        background-color: @BarColor;
        height: 12px;
    }
</style>

@code {
    [Parameter]
    public ActiveSkill Skill { get; set; } = new(DefaultAttack.Skill);

    public double CalculateProgress()
    {
        double percentage = Skill.Timer.Remaining / (Skill.Timer.CooldownDuration > 0.0 ? Skill.Timer.CooldownDuration : 1.0);
        return Skill.CurrentState switch
        {
            SkillTimer.State.Charging => 1.0 - percentage,
            SkillTimer.State.Cooldown => percentage,
            _ => 0.0
        };
    }

    string BarColor => Skill.CurrentState switch
    {
        SkillTimer.State.Charging => "var(--color-attack-progress)",
        SkillTimer.State.Active => "var(--color-active)",
        _ => "var(--color-inactive)"
    };

}
