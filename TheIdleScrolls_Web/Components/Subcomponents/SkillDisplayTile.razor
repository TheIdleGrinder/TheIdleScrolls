@using TheIdleScrolls_Core
@using TheIdleScrolls_Core.Components
@using TheIdleScrolls_Core.Modifiers
@using TheIdleScrolls_Core.Skills
@using TheIdleScrolls_Web.CoreWrapper

@inject CoreWrapperModel CoreWrapper

<div class="skill-display-frame">
    @if (Skill.NotUsableReason != "")
    {
        <span class="skill-display-not-usable-reason">@Skill.NotUsableReason</span>
    }
    <span class="skill-display-name">@Skill.Name</span>
    <span class="skill-display-tags">@(String.Join(", ", Skill.SkillTags.Select(t => t.Localize())))</span>
    <div class="skill-display-main">
        @if (Skill.ChargingTime > 0)
        {
            <span>Charging Time: <em>@Skill.ChargingTime.ToString("0.##") seconds</em></span>
        }
        @if (Skill.Timer.ActiveDuration > 0)
        {
            <span>Active Duration: <em>@Skill.Timer.ActiveDuration.ToString("0.##") seconds</em></span>
        }
        @if (Skill.Timer.CooldownDuration > 0)
        {
            <span>Cooldown Duration: <em>@Skill.Timer.CooldownDuration.ToString("0.##") seconds</em></span>
        }
        @if (Skill.Range > 0)
        {
            <span>Range: <em>@Skill.Range.ToString("0.#") m</em></span>
        }

        @if (Skill.ChargingStartEffects.Count > 0)
        {
            <em><u>On Charging:</u></em>
            @foreach (var effect in Skill.ChargingStartEffects)
            {
                <div class="skill-display-effect">
                    @foreach (var line in effect.Description.Split('\n'))
                    {
                        <span style="padding-left:@(10 * TabsAtStart(line))px;">
                            @(TabsAtStart(line) > 0 ? "➤ " : "")
                            @line
                        </span>
                    }
                </div>
            }
        }
        @if (Skill.ChargingWhileInEffects.Count > 0 || Skill.ChargingRepeatedEffect is not null)
        {
            <em><u>While Charging:</u></em>
            @foreach (var effect in Skill.ChargingWhileInEffects)
            {
                <div class="skill-display-effect">
                    @foreach (var line in effect.Description.Split('\n'))
                    {
                        <span style="padding-left:@(10 * TabsAtStart(line))px;">
                            @(TabsAtStart(line) > 0 ? "➤ " : "")
                            @line
                        </span>
                    }
                </div>
            }
            @if (Skill.ChargingRepeatedEffect is not null)
            {
                <div class="skill-display-effect">
                    @foreach (var line in Skill.ChargingRepeatedEffect.Description.Split('\n'))
                    {
                        <span style="padding-left:@(10 * TabsAtStart(line))px;">
                            @(TabsAtStart(line) > 0 ? "➤ " : "")
                            @line
                        </span>
                    }
                </div>
            }
        }

        @if (Skill.ActivityStartEffects.Count > 0)
        {
            <em><u>On Activation:</u></em>
            @foreach (var effect in Skill.ActivityStartEffects)
            {
                <div class="skill-display-effect">
                    @foreach (var line in effect.Description.Split('\n'))
                    {
                        <span style="padding-left:@(10 * TabsAtStart(line))px;">
                            @(TabsAtStart(line) > 0 ? "➤ " : "")
                            @line
                        </span>
                    }
                </div>
            }
        }
        @if (Skill.ActivityWhileInEffects.Count > 0 || Skill.ActivityRepeatedEffect is not null)
        {
            <em><u>While Active:</u></em>
            @foreach (var effect in Skill.ActivityWhileInEffects)
            {
                <div class="skill-display-effect">
                    @foreach (var line in effect.Description.Split('\n'))
                    {
                        <span style="padding-left:@(10 * TabsAtStart(line))px;">
                            @(TabsAtStart(line) > 0 ? "➤ " : "")
                            @line
                        </span>
                    }
                </div>
            }
            @if (Skill.ActivityRepeatedEffect is not null)
            {
                <div class="skill-display-effect">
                    @foreach (var line in Skill.ActivityRepeatedEffect.Description.Split('\n'))
                    {
                        <span style="padding-left:@(10 * TabsAtStart(line))px;">
                            @(TabsAtStart(line) > 0 ? "➤ " : "")
                            @line
                        </span>
                    }
                </div>
            }
        }

        @if (Skill.CooldownStartEffects.Count > 0)
        {
            <em><u>On End of Duration:</u></em>
            @foreach (var effect in Skill.CooldownStartEffects)
            {
                <div class="skill-display-effect">
                    @foreach (var line in effect.Description.Split('\n'))
                    {
                        <span style="padding-left:@(10 * TabsAtStart(line))px;">
                            @(TabsAtStart(line) > 0 ? "➤ " : "")
                            @line
                        </span>
                    }
                </div>
            }
        }
        @if (Skill.CooldownWhileInEffects.Count > 0 || Skill.CooldownRepeatedEffect is not null)
        {
            <em><u>During Cooldown:</u></em>
            @foreach (var effect in Skill.CooldownWhileInEffects)
            {
                <div class="skill-display-effect">
                    @foreach (var line in effect.Description.Split('\n'))
                    {
                        <span style="padding-left:@(10 * TabsAtStart(line))px;">
                            @(TabsAtStart(line) > 0 ? "➤ " : "")
                            @line
                        </span>
                    }
                </div>
            }
            @if (Skill.CooldownRepeatedEffect is not null)
            {
                <div class="skill-display-effect">
                    @foreach (var line in Skill.CooldownRepeatedEffect.Description.Split('\n'))
                    {
                        <span style="padding-left:@(10 * TabsAtStart(line))px;">
                            @(TabsAtStart(line) > 0 ? "➤ " : "")
                            @line
                        </span>
                    }
                </div>
            }
        }

    </div>
</div>

<style>
    .skill-display-frame {
        padding: 5px;
        margin-bottom: 0.5rem;
        display: flex;
        flex-direction: column;
        align-items: start;
        width: fit-content;
        background-color: var(--background-secondary);
    }

    .skill-display-name {
        display: inherit;
        font-weight: bold;
        font-size: 1rem;
        align-self: center;
    }

    .skill-display-not-usable-reason {
        display: inherit;
        color: red;
        align-self: center;
    }

    .skill-display-tags {
        display: inherit;
        font-size: 0.75rem;
        font-style: italic;
        color: gray;
        align-self: center;
    }

    .skill-display-main {
        display: flex;
        flex-direction: column;
        align-items: start;
    }

    .skill-display-effect {
        display: flex;
        flex-direction: column;
        align-items: start;
        font-size: 0.9rem;
    }

</style>

@code {
    [Parameter]
    [EditorRequired]
    public ActiveSkill Skill { get; set; }

    int TabsAtStart(string line)
    {
        int count = 0;
        foreach (char c in line)
        {
            if (c == '\t')
            {
                count++;
            }
            else
            {
                break;
            }
        }
        return count;
    }
}
